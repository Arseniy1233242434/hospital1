<Page x:Class="WpfApp3.Pages.AppointmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp3.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AppointmentPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid x:Name="AddPatient">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>


            </Grid.RowDefinitions>
           
            <Label Content="Дата приёма" Grid.Row="0"></Label>
            <DatePicker Grid.Row="0" Margin="100,0,0,0" Height="25" Width="180" Text="{Binding Path=Appointment.Date, Mode=TwoWay}"></DatePicker>

            <Label Content="Диагноз" Grid.Row="1"></Label>
            <TextBox Grid.Row="1" Margin="60,0,0,0" Height="25" Width="220" Text="{Binding Path=Appointment.Diagnosis, Mode=TwoWay}"></TextBox>
            <Label Content="Рекомендации" Grid.Row="2"></Label>
            <TextBox Grid.Row="2" Margin="60,0,0,0" Height="25" Width="220" Text="{Binding Path=Appointment.Recomendations, Mode=TwoWay}"></TextBox>


            <Button Content="Сохранить" Grid.Row="3" Click="Button_Click"></Button>
            <Button Content="Назад" Grid.Row="4" Click="Button_Click_1"></Button>
        </Grid>
        <Grid Grid.Column="1" >
            <Grid.RowDefinitions>
                
                <RowDefinition>

                </RowDefinition>
            </Grid.RowDefinitions>
            
            <ListView
Grid.Row="1" 
ItemsSource="{Binding  Patient.Appointments}"  
                >
                <ListView.ItemTemplate >
                    <DataTemplate>
                        <StackPanel>
                            
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Дата приёма: "/>
                                <TextBlock Text="{Binding Date}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Доктор: "/>
                                <TextBlock Text="{Binding DoctorInfo}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Диагноз: "/>
                                <TextBlock Text="{Binding Diagnosis}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Рекомендации: "/>
                                <TextBlock Text="{Binding Recomendations}"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
