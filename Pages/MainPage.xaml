<Page x:Class="WpfApp3.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp3.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid  Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="34"></RowDefinition>
                <RowDefinition Height="33"></RowDefinition>
                <RowDefinition Height="33"></RowDefinition>

                <RowDefinition Height="38"></RowDefinition>
                <RowDefinition Height="38"></RowDefinition>
                <RowDefinition Height="38"></RowDefinition>
            </Grid.RowDefinitions>
            <Label Content="Фамилия:" Grid.Row="0"></Label>
            <Label Content="{Binding Path=Doctor1.LastName, Mode=OneWay}" Grid.Row="0" Margin="60,0,0,0"></Label>

            <Label Content="Имя:" Grid.Row="1"></Label>
            <Label Content="{Binding Path=Doctor1.Name, Mode=OneWay}" Grid.Row="1" Margin="60,0,0,0"></Label>

            <Label Content="Отчество:" Grid.Row="2"></Label>
            <Label Content="{Binding Path=Doctor1.MiddleName, Mode=OneWay}" Grid.Row="2" Margin="70,0,0,0"></Label>

            <Label Content="Специализация:" Grid.Row="3"></Label>
            <Label Content="{Binding Path=Doctor1.Specialization, Mode=OneWay}" Grid.Row="3" Margin="100,0,0,0"></Label>

            <Label Content="пациентов:" Grid.Row="4"></Label>
            <Label Content="{Binding Path= patientcount.ID, Mode=OneWay}" Grid.Row="4" Margin="100,0,0,0"></Label>

            <Label Content="Докторов:" Grid.Row="5"></Label>
            <Label Content="{Binding Path=patientcount.Password, Mode=OneWay}" Grid.Row="5" Margin="100,0,0,0"></Label>
        </Grid>
        <Grid Grid.Column="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="60">
                    
                </RowDefinition>
                <RowDefinition>

                </RowDefinition>
            </Grid.RowDefinitions>
            <Button Content="Создать пациента" Width="100" Height="40" Margin="0,0,290,0" Click="Create_Patient"></Button>
            <Button Content="Начать прием" Width="90" Height="40" Margin="0,0,90,0" Click="Button_Click"></Button>
            <Button Content="Изменить информацию" Width="150" Height="40" Margin="160,0,0,0" Click="SavePatient"></Button>
            <ListView
Grid.Row="1" 
ItemsSource="{Binding  Patientss}"  
                SelectedItem="{Binding SelectedUser}">
                <ListView.ItemTemplate >
                    <DataTemplate>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Id: "/>
                                <TextBlock Text="{Binding Id}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Имя: "/>
                                <TextBlock Text="{Binding Name}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Фамилия: "/>
                                <TextBlock Text="{Binding LastName}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Отчество: "/>
                                <TextBlock Text="{Binding MiddleName}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Возраст: "/>
                                <TextBlock Text="{Binding Birthday}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Контактный телефон: "/>
                                <TextBlock Text="{Binding PhoneNumber}"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
